# Agent Rules
- **Security**: Run `claude /shannon` (Security Scan) before every push.
- **Docs**: Auto-update README.md and CHANGELOG.md on every release.
- **Versioning**: Increment version in package.json (v1.0.0).
- **Sync**: Use `gh release create` to finalize every project.
